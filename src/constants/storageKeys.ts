// AsyncStorage keys shared across more than one screen/component -- kept
// here instead of as ad hoc string literals so the two ends can't drift.

/** Set once the Home "How to check a product" card has been dismissed. */
export const SEEN_SCAN_HELP_KEY = 'seenHomeScanHelp';
